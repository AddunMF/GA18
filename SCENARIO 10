<?php
// ERROR: wrong variable name results to undefined variable error
// $sql = "SELECT * FROM students WHERE age = $aeg";

$age = $_POST['age'];

// CORRECT: use correct variable name on SQL query
$sql = "SELECT * FROM students WHERE age = $age";
$res = mysqli_query($conn, $sql);

/*
checked and change the mispelled variable  to avoid 'undefined variable error' error.
*/
?>
