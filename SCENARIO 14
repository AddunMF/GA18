<?php
$data = $_POST;

// ERROR: missing qoutes, used $data directly
// $sql = "INSERT INTO students (first_name, last_name, email)
// VALUES ($data[first_name], $data[last_name], $data[email])";

// CORRECT: add missing qoutes and proper indexing
$first = $data['first_name'];
$last = $data['last_name'];
$email = $data['email'];

$sql = "INSERT INTO students (first_name, last_name, email)
VALUES ('$first', '$last', '$email')";
$res = mysqli_query($conn, $sql);

/*
use proper array indexing using qoutes ['first_name'], ['last_name'], ['email'] 
a value without a qoute will display an undefined index error
*/
?>
